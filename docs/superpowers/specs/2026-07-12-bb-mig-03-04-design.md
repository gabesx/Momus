# BB-MIG-03/04 Design — Settings migration + parallel-run diffs

**Date:** 2026-07-12  
**Branch:** `feat/bb-mig-03-04` (from master after #9)

## BB-MIG-03

1. Upsert `bug_budget_config` from legacy PHP (`bugbudget.php`, `bugbudget-projects.php`): multipliers, budgets, mappings, exclusions. Preserve D-2 typos/dup keys as PHP last-wins.
2. Upsert non-secret `settings`: `jira_url`, `jira_username`, `jira_enabled` from MySQL. **Never** copy `jira_api_token`.
3. Print manual token re-entry checklist (Vault / Settings UI).
4. Document `DROP TABLE bug_budget_settings` for legacy cutover (do not auto-drop).

## BB-MIG-04

CLI `diff` comparing legacy MySQL ↔ Momus Postgres using `@momus/domain`:

- Counts + per-year + checksum (reuse reconcile)
- Open Bug / Open Defect summaries (current year + all years) — stable JSON fingerprint
- CSV body hash via `buildBugBudgetCsv`

Exit 1 on hard mismatches.
