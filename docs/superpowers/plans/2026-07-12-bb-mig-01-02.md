# BB-MIG-01/02 Implementation Plan

> **For agentic workers:** executed inline in session 2026-07-12.

**Goal:** Bulk-copy legacy `bug_budget` (+ sync runs + cron row) into Momus and reconcile.

**Architecture:** `@momus/migration` CLI — mysql2 → transform → pg; `copy` + `reconcile`.

**Tech Stack:** TypeScript, mysql2, pg, Vitest

---

### Done

- [x] Design doc + runbook
- [x] Transform/checksum unit tests
- [x] Copy CLI with `--truncate-target`
- [x] Reconcile COUNT / per-year / checksum
- [x] Local run against `qara@3307` → Momus `54422` — **ok: 4653, checksum match**
